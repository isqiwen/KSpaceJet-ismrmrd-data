# Shared licence material

Each dataset must keep its authoritative upstream terms in
`datasets/<dataset-id>/LICENSE.txt`. This directory is reserved for licence
texts or notices genuinely shared by more than one dataset.

Do not move or substitute a dataset's original licence merely because a similar
licence is already present here. Record the source URL and licence identifier in
that dataset's `dataset.yaml` as well.
